<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kensara.stationcarcontrol">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".UtilisateurActivity" />
        <activity
            android:name=".SuperViseurActivity"
            android:label="@string/title_activity_super_viseur"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SuperAdminActivity"
            android:label="@string/title_activity_super_admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Fragments.CreateUserActivity"
            android:label="@string/title_activity_create_user"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>